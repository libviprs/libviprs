# capture.py regenerates every fixture and every intermediate from the vips
# binary and from the two reference `sample.hdr` files named in it, so only
# capture.py, commands.sh and oracle.json are pinned here.
fixtures/
outputs/
